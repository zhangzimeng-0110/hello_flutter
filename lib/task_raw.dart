import 'package:flutter/material.dart';

class task_raw_button extends StatelessWidget {
  const task_raw_button({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
